# 更新日志

## 2.2.0 (2026-07-18)

### 收藏页重构
- 收藏容量上限从 20 条提升至 150 条，支持收藏 A-Z 首字母分类筛选
- 收藏列表分页加载，已适配多屏布局

### 大键盘输入
- 搜索框三击切换完整 QWERTY 大键盘，每次敲击触感震动反馈
- 大键盘预览文本根据屏幕尺寸自适应跑马灯

### 搜索页优化
- 键盘收起后显示提示文字"清空即可退出"，引导明确
- 搜索框拖尾文本跑马灯支持按屏幕安全宽度适配

### 性能提升
- 搜索结果缓存扩容，标签计算推迟至渲染时
- `wrapText` 命中缓存后短路返回

### 结果页改进
- 实现 C 方案分页(上一页/下一页)，翻页后列表滚动至顶部/底部
- 动态增量渲染结果卡片（1s 轮询 + OOM 防护）
- 分页状态提示文字修复，中文单字查询跳过索引扫描
- 搜索时暂停跑马灯减少卡顿
- 继承基础词音标，拼写自动纠错更准确

### 代码清理
- 删除构建/缓存产物（build、dist、dict shards）
- 移除一次性分析脚本和日志文件

---

## 2.1.0 (2026-07-17)

### 关于页多屏适配
- 关于页在所有屏幕尺寸(iWatch/手环长条/466圆屏)下自适应排版
- 安全宽度复用结果页参数，确保文字不溢出

### 词典引擎升级
- 迁移至 compact-v3 格式，索引文件大幅瘦身
- 移除废弃的 inflect_eid/ireverse_eid 文件
- 搜索结果返回上限 20 条

### 输入法多屏稳定
- 多屏输入法稳定版 1.0，胶囊屏/圆屏/方屏布局自适应
- 搜索键盘隐藏时保留状态，输入安全区内滑动手势不冲突

### 详情页增强
- 底部按钮按屏幕尺寸布局（大屏并排，小屏堆叠）
- 集成 WordNet 派生词表，大幅扩展相关词覆盖

### 结果页改进
- 静态文本 + 跑马灯单槽模式，超长单词完整显示
- 首页非胶囊屏幕显示词典图标

---

## 2.0.0 — 《体积压缩 & 多屏适配》 (2026-07-14)

### 词典体积压缩
- 重写字典生成脚本：词条分片、中文索引按 Unicode 分桶、增量 delta-base36 编码
- 新增英文屈折变化索引（inflect/ 和 inflect_reverse/）
- 运行时资源体积减少约 60%

### 多屏适配（核心）
- 首页布局自适应：胶囊屏 4 按钮并排，圆屏卡片置于内切正方形
- 搜索页面输入框/候选区/键盘按屏幕比例缩放
- 结果页和详情页适配 212×520 / 294×126 / 466×466 三种尺寸
- 自定义 `getCharWidth` 和 `wrapText` 函数按不同字体大小和屏幕宽度计算

### 稳定性
- 多设备间打开搜索不崩溃，解决 HAP 调度时序问题
- 所有 `_destroyed` 回调守卫替换为 Vela 兼容写法
- 修复因 `@media` 查询（Vela 不支持）导致的原生 Data Abort
- 页面跳转时序保护：延迟 `autoSearch`、watch 清理、异步回调守卫
- 启用 JSC 字节码编译提升启动性能

### 路由规范
- records → search 使用 `router.replace` + `autoSearch="1"`
- detail → results（屈折跳转）使用 `router.replace`
- 全局滑动手势翻页（每页判断条件一致）

---

## 1.0.0 — 初始版本 (2026-07-08)

### 核心功能
- 英汉/汉英双向查词，支持 15,000+ 英文词头、128,000+ 中文短语
- 模糊搜索（编辑距离 ≤2），自动纠错
- 英文联想输入（首字母索引 + tag 排序）

### 界面
- 暗色主题（#020813 / #000000），蓝/白配色
- 英语 QWERTY 键盘输入法组件
- 搜索历史 + 收藏夹（各 20 条，`@system.storage`）
- 单词详情页：释义、音标、派生词、收藏/分享

### 工程基础
- 小米 Vela QuickApp 工程结构（SFC .ux 文件格式）
- prettier + stylelint + eslint + commitlint 工具链
- `scripts/generate_watch_dict.py` 词典生成流水线
- `scripts/deploy_watch.ps1` 部署脚本
